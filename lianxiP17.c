#include<stdio.h>
#include<stdlib.h>

int gcd(int,int);
 
 int main(void )
 {
 int result;
 result=gcd(150,35);
 printf("the gcd of 150 and 30 is %i\n",result) ;
 result=gcd(1026,405)
 /*printf("the gcd of 1026 and 405 is %i\n",result);
 printf("the gcd of 83 and 240 is %\n",gcd(83,240));
 system("PAUSE");*/
 return 0; 
}
